<!DOCTYPE html>
<html>
<head>
  <title>Empower Your Life - Supplimental Materials</title>
  <!-- Latest compiled and minified CSS & JS -->
  <!-- <link rel="stylesheet" media="screen" href="//netdna.bootstrapcdn.com/bootstrap/3.3.4/css/bootstrap.min.css"> -->
  <%= stylesheet_link_tag    'application', media: 'all', 'data-turbolinks-track' => true %>
  <link rel="stylesheet" href="//cdnjs.cloudflare.com/ajax/libs/bootswatch/3.3.5/cerulean/bootstrap.min.css">

  <%= javascript_include_tag 'application', 'data-turbolinks-track' => true %>
  <%= include_gon(:init => true) %>
  <script src="//netdna.bootstrapcdn.com/bootstrap/3.3.4/js/bootstrap.min.js"></script>
  <%= csrf_meta_tags %>
</head>
<body>
<nav class="navbar navbar-default" role="navigation">
  <!-- Brand and toggle get grouped for better mobile display -->
  <div class="navbar-header">
    <button type="button" class="navbar-toggle" data-toggle="collapse" data-target=".navbar-ex1-collapse">
      <span class="sr-only">Toggle navigation</span>
      <span class="icon-bar"></span>
      <span class="icon-bar"></span>
      <span class="icon-bar"></span>
    </button>
    <a class="navbar-brand" href="/">Empower Your Life - Supplemental Materials</a>
  </div>

  <!-- Collect the nav links, forms, and other content for toggling -->
  <div class="collapse navbar-collapse navbar-ex1-collapse">

    <ul class="nav navbar-nav navbar-right">

        <li class="dropdown">
          <% if @current_user %>
            <a href="#" class="dropdown-toggle" data-toggle="dropdown"><%= @current_user.name %> <b class="caret"></b></a>
            <ul class="dropdown-menu">
            <% if @current_user[:kind] == 'admin' %>
              <li><%= link_to "Add New Materials", new_material_path %></li>
              <li><%= link_to "Materials", materials_path %></li>
              <li><%= link_to "Users", user_admin_index_path %></li>
            <% end %>
            <% if @current_user[:kind] != 'admin' %>
              <li><%= link_to "My Materials", user_path %></li>
            <% end %>
            <li role="separator" class="divider"></li>
            <li><%= link_to "Log out", logout_path %></li>
          <% end %>
          </ul>
        </li>
    </ul>
  </div><!-- /.navbar-collapse -->
</nav>
<%= image_tag("banner.jpg", width: "350") %>
<%= render 'layouts/flash' %>
<%= yield %>

</body>
</html>
